/*
 * Public runtime config (safe to commit).
 * Set apiBaseUrl to your deployed backend when frontend is hosted separately
 * (for example GitHub Pages -> Render/Fly/Railway API).
 */
window.__APP_CONFIG = {
    apiBaseUrl: ''
};
